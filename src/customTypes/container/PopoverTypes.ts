
export interface AddItemPopoverProps<T> {
    // title: string
    // description: string
    // openButtonLabel: string
    // submitButtonText: string
    children: React.ReactNode
    callback: null
    // handleSubmitCallback: (event: any) => Promise<void>
}